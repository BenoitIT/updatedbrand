import { FC } from 'react';
import styles from '../styles/comps/AboutSection.module.scss';

const AboutSection: FC = () => {
  return <div className={styles.aboutSection}>ABOUT</div>;
};

export default AboutSection;
